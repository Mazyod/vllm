# Opt-in candidate; merge AFTER stack.yaml. The other model is unchanged.
version: '3.8'
services:
  deepseek:
    volumes:
    - type: tmpfs
      target: /dev/shm
      tmpfs:
        size: 343597383680
configs:
  deepseek:
    name: dedicated-deepseek-ramkv-${DEEPSEEK_RAM_KV_CONFIG_REV:-20260917a}
    file: ./ram-kv/deepseek.yaml
